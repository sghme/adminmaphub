import 'package:flutter/material.dart';

class UserLogs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('User Logs')),
      body: Center(
        child: Text('User Logs ni'),
      ),
    );
  }
}
